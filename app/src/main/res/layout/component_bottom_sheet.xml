<?xml version="1.0" encoding="utf-8"?>
<layout>
  <RelativeLayout
      xmlns:android="http://schemas.android.com/apk/res/android"
      android:layout_width="match_parent"
      android:layout_height="match_parent"
      android:paddingBottom="@dimen/activity_vertical_margin"
      android:paddingLeft="@dimen/activity_vertical_margin"
      android:paddingRight="@dimen/activity_vertical_margin"
      android:paddingTop="@dimen/activity_vertical_margin"
      >

    <TextView
        android:id="@+id/bottom_sheet_reading"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/book_reading"
        />

    <TextView
        android:id="@+id/bottom_sheet_to_read"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/bottom_sheet_reading"
        android:layout_marginTop="@dimen/space_large"
        android:text="@string/book_to_read"
        />

    <TextView
        android:id="@+id/bottom_sheet_read"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/bottom_sheet_to_read"
        android:layout_marginTop="@dimen/space_large"
        android:text="@string/book_read"
        />

    <TextView
        android:id="@+id/bottom_sheet_quitted"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/bottom_sheet_read"
        android:layout_marginTop="@dimen/space_large"
        android:text="@string/book_quitted"
        />

  </RelativeLayout>
</layout>